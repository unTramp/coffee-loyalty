# PostgreSQL
DATABASE_URL=postgres://coffee:coffee@localhost:5433/coffee_loyalty

# Redis
REDIS_URL=redis://localhost:6380

# JWT
JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-refresh-secret

# HMAC secret for QR signing (per-shop, but default for seed)
HMAC_SECRET=change-me-hmac-secret

# Telegram Bot
BOT_TOKEN=your-telegram-bot-token
BOT_SECRET=random-webhook-secret

# Resend (email OTP)
RESEND_API_KEY=re_xxx
EMAIL_FROM=onboarding@resend.dev

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173

# API URL (for client/bot)
API_URL=http://localhost:3000
